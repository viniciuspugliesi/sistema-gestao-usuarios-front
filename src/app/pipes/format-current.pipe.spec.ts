import { FormatCurrentPipe } from './format-current.pipe';

describe('FormatCurrentPipe', () => {
    it('create an instance', () => {
        const pipe = new FormatCurrentPipe();
        expect(pipe).toBeTruthy();
    });
});
