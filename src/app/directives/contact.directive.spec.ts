import { ContactDirective } from './contact.directive';

describe('ContactDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactDirective();
    expect(directive).toBeTruthy();
  });
});
